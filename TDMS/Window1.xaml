<Window x:Class="TDMS.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TDMS"
        mc:Ignorable="d" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        AllowsTransparency="True" 
        Background="Transparent" 
        Height="500" 
        Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <Border CornerRadius="10 0 0 10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3AA9AD" Offset="0" />
                    <GradientStop Color="#3AADA1" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <Border Background="#FFFFFF" Grid.Column="1" CornerRadius="0 10 10 0"/>

        <StackPanel Orientation="Vertical">
            <Grid Height="60" VerticalAlignment="Top" Margin="0,30,0,0">
                <TextBlock Text="LOGO HERE" Foreground="White" FontSize="15" TextWrapping="WrapWithOverflow" TextAlignment="Center" Padding="1,0,1,0" VerticalAlignment="Center"/>
            </Grid>

            <Menu Height="300" Background="Transparent">
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>
                <MenuItem Header="DASHBOARD" Foreground="White" HorizontalAlignment="Center">

                </MenuItem>

                <MenuItem Header="JOBS" Foreground="White" HorizontalAlignment="Center">

                </MenuItem>

                <MenuItem Header="MATERIALS" Foreground="White" HorizontalAlignment="Center">

                </MenuItem>

                <MenuItem Header="EMPLOYEE" Foreground="White" HorizontalAlignment="Center">

                </MenuItem>

                <MenuItem Header="SIGN OUT" Foreground="White" HorizontalAlignment="Center" Click="MenuOut_Click">

                </MenuItem>

            </Menu>
        </StackPanel>

    </Grid>
</Window>
